// X/Twitter gets the same card as Open Graph. The metadata previously pointed at
// /twitter-image.png, a file that never existed (the asset was named twitter-img.png).
export { default, alt, size, contentType } from "./opengraph-image";
